<%= turbo_stream.replace dom_id(@publication) do %>
  <%= turbo_frame_tag dom_id(@publication) do %>
    <%= render "publications/form", profile: nil, publication: @publication, cancel: publication_url(@publication, format: 'turbo_stream') %>
  <% end %>
<% end %>