<%= turbo_frame_tag dom_id(award), "data-sortable-url" => award_path(award, format: 'json') do %>
  <div class="list-group-item">
    <div class="row">
      <div class="col-md-8">
        <h4 class="h5 mb-0"><%= award.t_title %></h4>
        <p class="text-muted small mb-0">
          <%= award.year %> – <%= award.issuer %>
        </p>
      </div>
      <div class="col-md-4 text-right">
        <%= render "shared/item_actions", item: award %>
      </div>
    </div>
  </div>
<% end %>
