<%= form_with(
  model: item, 
  remote: true, 
  format: 'json',
  class: "visibility-radios",
  data: {
    controller: "visibility" 
  }) do |form| %>
  <%= audience_selector(form, with_stimulus: true, with_wrapper: false) %>
<% end %>
